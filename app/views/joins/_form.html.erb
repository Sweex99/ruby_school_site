<%= form_with(model: join, local: true) do |form| %>
  <% if join.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(join.errors.count, "error") %> prohibited this join from being saved:</h2>

      <ul>
      <% join.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
